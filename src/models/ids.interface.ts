export class IDs {
    
    private project_id: string;
    private _id: string;

    constructor(project_id: string, _id: string){
        this._id = _id;
        this.project_id = project_id;
    }
}